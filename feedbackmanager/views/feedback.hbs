<h1>Anonymous Speech Feedback System</h1>
<h3>To Send Your Anonymous Feedback</h3>
<h3>input your feedback below</h3>
<div>
  <div class="form-group" contenteditable="true" onKeyUp="state.feedback = this.innerHTML"></div>
  <div class="form-group">
    <button type="button" class="btn btn-primary" onClick="sendFeedback()">Get Feedback Link</button>
  </div>
</div>
<script>
  var state = {};
  async function req(params) {
    const url = 'http://localhost:3000'
    const response = await fetch(url + params.endpoint, {
      method: 'POST', // *GET, POST, PUT, DELETE, etc.
      mode: 'cors', // no-cors, *cors, same-origin
      cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
      credentials: 'same-origin', // include, *same-origin, omit
      headers: { 'Content-Type': 'application/json' },
      redirect: 'follow', // manual, *follow, error
      referrerPolicy: 'no-referrer', // no-referrer, *no-referrer-when-downgrade, origin, origin-when-cross-origin, same-origin, strict-origin, strict-origin-when-cross-origin, unsafe-url
      body: JSON.stringify(params.data) // body data type must match "Content-Type" header
    });
    return response.json(); // parses JSON response into native JavaScript objects
  }
  async function sendFeedback() {
    const res = await res({
      data: {
        feedback: this.state.feedback,
        token: new URLSearchParams(window.location.href).get('token')
      },
      endpoint: '/sendFeedback'
    });
    console.log(res);
  }
</script>
